package engine_test

/*
import (
	"net/http"
)

var onlineQueries = []struct {
	query  string
	input  string
	status int
	output string
}{
	{"x = opa.runtime()", "{}", http.StatusOK, `(?s){.*commit.*config.*bundles.*.*env.*PATH.*version.*}`},
	{"x = dir.foobar()", "{}", http.StatusBadRequest, `(?s) query failed: failed to evaluate rego query: 1 error occurred: 1:5: rego_type_error: undefined function dir.foobar`},
}
*/
